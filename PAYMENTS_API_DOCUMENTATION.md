# Aviv POS API - סיכום פרמטרי התשלומים

## סקירה כללית

ה-API של Aviv POS תומך במספר סוגי תשלומים. בתוסף שלנו, אנו משתמשים רק ב-**PREPAID** (תשלום מראש) עם טוקן אשראי, אך ה-API תומך גם בסוגים נוספים.

---

## סוגי תשלומים ב-API

### 1. **PrepaidPayment** (תשלום מראש)
- **paymentType**: `"PREPAID"`
- **תיאור**: תשלום שכבר שולם באתר
- **פרמטרים**:
  - `amount` (חובה): סכום באגורות (מספר שלם)
  - `card` (אופציונלי): אובייקט עם `token` - טוקן אשראי
  - `prepaid` (אופציונלי): `true` - מציין שכבר נגבה

### 2. **PostpaidPayment** (תשלום מאוחר)
- **paymentType**: `"PREPAID"` (כן, גם זה!)
- **תיאור**: תשלום שלא שולם באתר, ייגבה בקופה/שליח
- **פרמטרים**:
  - `amount` (חובה): סכום באגורות
  - `card` (חובה): אובייקט עם `token` - טוקן לחיוב מאוחר
  - `prepaid`: `false` - מציין שעדיין לא נגבה

### 3. **CashPayment** (מזומן)
- **paymentType**: `"CASH"`
- **תיאור**: תשלום במזומן בקופה
- **פרמטרים**:
  - `amount` (חובה): סכום באגורות

### 4. **BankTransferPayment** (העברה בנקאית)
- **paymentType**: `"BANK_TRANSFER"`
- **תיאור**: תשלום בהעברה בנקאית
- **פרמטרים**:
  - `amount` (חובה): סכום באגורות
  - `payerID` (חובה): מספר תעודת זהות של המשלם
  - `payerName` (חובה): שם המשלם

---

## איך התוסף שלנו עובד

### לוגיקה נוכחית

התוסף שלנו משתמש בשני סוגי תשלומים:

#### מצב 1: **ישולם באתר** (`CASH` mode)
```json
{
  "paymentType": "CASH",
  "amount": 13990
}
```

**מתי נשלח:**
- הלקוח שילם באתר (כל שיטת תשלום)
- ההגדרה ב-admin: "ישולם באתר"

**מה קורה:**
- התשלום כבר נגבה באתר
- נשלח כ-CASH payment type
- **לא** נשלח טוקן כרטיס (אפילו אם קיים)
- ה-POS רק רושם שהתשלום כבר נגבה

---

#### מצב 2: **ישולם בקופה** (`PREPAID` mode)
```json
{
  "paymentType": "PREPAID",
  "amount": 13990,
  "card": {
    "token": "36546404-6b58-4d13-918a-622dba4df5ae"
  },
  "prepaid": false
}
```

**מתי נשלח:**
- הלקוח הזמין אבל לא שילם באתר
- יש טוקן אשראי לחיוב מאוחר
- ההגדרה ב-admin: "ישולם בקופה"

**מה קורה:**
- התשלום **לא** נגבה באתר
- הטוקן נשלח ל-POS לחיוב בקופה/שליח
- ה-POS יחייב את הלקוח בעת המסירה עם הטוקן

---

## פרמטרים מפורטים

### `paymentType` (חובה)
- **ערכים אפשריים**: `"PREPAID"`, `"CASH"`, `"BANK_TRANSFER"`
- **בתוסף שלנו**: תמיד `"PREPAID"`

### `amount` (חובה)
- **סוג**: מספר שלם (integer)
- **יחידה**: אגורות (1 שקל = 100 אגורות)
- **דוגמה**: `13990` = 139.90 ₪

### `card` (אופציונלי)
- **סוג**: אובייקט או `null`
- **תת-פרמטרים**:
  - `token` (string): טוקן אשראי מ-Cardcom
- **מתי נשלח**: רק אם יש טוקן ב-order meta (`_CardcomToken` או `_CardcomTokenId`)

### `prepaid` (אופציונלי)
- **סוג**: boolean
- **ערכים**:
  - `true`: התשלום כבר נגבה באתר
  - `false`: התשלום ייגבה בקופה/שליח
- **בתוסף שלנו**: נקבע לפי ה-mode ב-admin settings

### `payerID` (רק ל-BANK_TRANSFER)
- **סוג**: string
- **תיאור**: מספר תעודת זהות של המשלם

### `payerName` (רק ל-BANK_TRANSFER)
- **סוג**: string
- **תיאור**: שם המשלם

---

## דוגמאות לכל סיטואציה

### דוגמה 1: תשלום מלא באתר (CASH)
```json
{
  "payments": [
    {
      "paymentType": "CASH",
      "amount": 13990
    }
  ]
}
```
**סיטואציה**: לקוח שילם 139.90 ₪ באתר (כל שיטת תשלום). התשלום נגבה, נשלח כ-CASH ללא טוקן.

---

### דוגמה 2: תשלום בקופה עם טוקן (PREPAID)
```json
{
  "payments": [
    {
      "paymentType": "PREPAID",
      "amount": 25000,
      "card": {
        "token": "754"
      },
      "prepaid": false
    }
  ]
}
```
**סיטואציה**: לקוח הזמין ב-250 ₪ אבל לא שילם באתר. הטוקן נשלח ל-POS לחיוב בעת המסירה.

---

### דוגמה 3: תשלום בקופה ללא טוקן (PREPAID)
```json
{
  "payments": [
    {
      "paymentType": "PREPAID",
      "amount": 5000,
      "prepaid": false
    }
  ]
}
```
**סיטואציה**: לקוח הזמין ב-50 ₪ אבל לא שילם באתר ואין טוקן. ייגבה בקופה במזומן או בכרטיס.

---

### דוגמה 4: תשלום מזומן בקופה (לא מיושם כרגע)
```json
{
  "payments": [
    {
      "paymentType": "CASH",
      "amount": 10000
    }
  ]
}
```
**סיטואציה**: תשלום במזומן בקופה. לא מיושם בתוסף שלנו כרגע.

---

### דוגמה 5: תשלום בהעברה בנקאית (לא מיושם כרגע)
```json
{
  "payments": [
    {
      "paymentType": "BANK_TRANSFER",
      "amount": 30000,
      "payerID": "123456789",
      "payerName": "יוסף שכמן"
    }
  ]
}
```
**סיטואציה**: תשלום בהעברה בנקאית. לא מיושם בתוסף שלנו כרגע.

---

## מיפוי ב-Admin Settings

בעמוד ההגדרות של התוסף, יש טבלת מיפוי תשלומים:

| WooCommerce Gateway | מצב חיוב | מה נשלח ל-API |
|---------------------|-----------|---------------|
| `cardcom` | ישולם באתר | `paymentType: "CASH"`, ללא `card` |
| `bacs` | ישולם באתר | `paymentType: "CASH"`, ללא `card` |
| `cardcom` | ישולם בקופה | `paymentType: "PREPAID"`, `prepaid: false`, `card.token` (אם קיים) |
| `cod` | ישולם בקופה | `paymentType: "PREPAID"`, `prepaid: false`, ללא `card` |

---

## הערות חשובות

1. **שני סוגי תשלומים:**
   - **CASH** (`"ישולם באתר"`): תשלום שכבר נגבה באתר, נשלח כ-CASH ללא טוקן
   - **PREPAID** (`"ישולם בקופה"`): תשלום שייגבה בקופה, נשלח כ-PREPAID עם טוקן (אם קיים)

2. **טוקן אשראי:**
   - נשלח רק במצב "ישולם בקופה" (PREPAID)
   - מחפש: `_CardcomToken` או `_CardcomTokenId`
   - במצב "ישולם באתר" (CASH) - **לא** נשלח טוקן (אפילו אם קיים)

3. **סכום באגורות:**
   - כל הסכומים נשלחים באגורות (מספר שלם)
   - דוגמה: 139.90 ₪ = `13990`

4. **תשלומים מרובים:**
   - ה-API תומך במערך של תשלומים
   - כרגע התוסף שולח תשלום אחד בלבד

---

## קוד רלוונטי

הפונקציה `map_payments()` ב-`class-oc-aviv-pos-order-handler.php`:
- קובעת את ה-`prepaid` flag לפי ה-mode ב-admin
- מחפשת טוקן ב-order meta
- בונה את ה-payload בהתאם

